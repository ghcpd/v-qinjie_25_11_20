# Insecure app - do not copy secrets here (demo only)
# This app intentionally includes a hardcoded API key inside client code for the demo. Do not put secrets in .env files committed to the repo.
INSECURE_CLIENT_KEY=INSECURE_CLIENT_API_KEY_12345
